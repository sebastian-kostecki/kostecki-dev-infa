# Email for Let's Encrypt certificate notifications
ACME_EMAIL=admin@kostecki.dev

# Landing deploy path on VPS
VPS_LANDING_DIR=/srv/apps/landing

# Wallet-master deploy path on VPS
VPS_WALLET_DIR=/srv/apps/wallet-master

# Domains (landing only — add more in each app's repo when needed)
DOMAIN=kostecki.dev
DOMAIN_WWW=www.kostecki.dev
